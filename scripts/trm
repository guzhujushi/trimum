#!/usr/bin/env bash
# trm CLI wrapper.
#
# The production installer copies this wrapper into /opt/trimum/bin/trm.
# It assumes that trimum-core is installed for the active python3 runtime.
set -euo pipefail

exec python3 -m trimum_core.main "$@"
